node_modules
# Keep environment variables out of version control.
# Ignore every .env file (incl. .env.development / .env.production),
# but keep the committed template.
.env
.env.*
!.env.example

/generated/prisma
