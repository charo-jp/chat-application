## Primary Keys: UUID or Auto-Increment?

### What is UUID?

UUID stands for Universal Unique Identifier.
It uses 128 bits (16 bytes) to uniquely identify resources across distributed systems.

### What is an Index?

- It can significantly improve query performance by allowing faster data retrieval.

```sql
CREATE INDEX index_name ON table_name;
```
